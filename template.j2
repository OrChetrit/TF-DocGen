# {{ module.module_name }}

<!-- BEGIN_TF_DOCS -->
> **Auto-generated by TF-DocGen. Do not edit manually.**

## Overview

This document describes the interface of the **`{{ module.module_name }}`** Terraform module.
{% if module.terraform_required_version %}

**Terraform version requirement:** `{{ module.terraform_required_version }}`
{% endif %}
{% if module.required_providers %}

### Required Providers

| Provider | Source / Version |
|----------|-----------------|
{% for name, attrs in module.required_providers.items() %}
{% if attrs is mapping %}
| `{{ name }}` | `{{ attrs.get('source', 'n/a') }}`{% if attrs.get('version') %} / `{{ attrs['version'] }}`{% endif %} |
{% else %}
| `{{ name }}` | `{{ attrs }}` |
{% endif %}
{% endfor %}
{% endif %}

---

## Resources

{% if module.resources %}
The following resources are managed by this module:

| Resource Type | Resource Name |
|---------------|---------------|
{% for res in module.resources %}
| `{{ res.resource_type }}` | `{{ res.resource_name }}` |
{% endfor %}
{% else %}
*No resources were detected in this module.*
{% endif %}

---

## Input Variables

{% if module.variables %}
| Name | Type | Default | Description | Required |
|------|------|---------|-------------|----------|
{% for var in module.variables %}
| `{{ var.name }}` | {{ var.type_display() }} | {{ var.sensitive_default_display() }} | {{ var.description if var.description else "*No description provided.*" }} | {{ "Yes" if not var.has_default else "No" }} |
{% endfor %}
{% else %}
*This module has no input variables.*
{% endif %}

---

## Outputs

{% if module.outputs %}
| Name | Description | Sensitive |
|------|-------------|-----------|
{% for out in module.outputs %}
| `{{ out.name }}` | {{ out.description if out.description else "*No description provided.*" }} | {{ "Yes" if out.sensitive else "No" }} |
{% endfor %}
{% else %}
*This module produces no outputs.*
{% endif %}

---

## Governance & Compliance

{% if module.governance_passed %}
**Status: COMPLIANT**

All input variables declare both a `type` and a `description`. This module meets
the organisation's infrastructure-as-code governance standard.
{% else %}
**Status: NON-COMPLIANT**. {{ module.non_compliant_variables | length }} variable(s) require attention.

The following variables are missing required governance attributes (`type` and/or `description`).
Enforce compliance by adding the missing attributes or by running `tf-docgen --strict` in CI.

| Variable | Issues |
|----------|--------|
{% for var in module.non_compliant_variables %}
| `{{ var.name }}` | {{ var.governance_issues | join(', ') }} |
{% endfor %}

> **Remediation:** Add the missing `description` and/or `type` attributes to each flagged
> variable in `variables.tf`, then re-run `tf-docgen`.
{% endif %}

---

## Usage

```hcl
module "{{ module.module_name }}" {
  source = "github.com/or-chetrit/terraform-aws-{{ module.module_name }}"
{% for var in module.variables if not var.has_default %}

  {{ var.name }} = # TODO: provide a value
{% endfor %}
}
```

Developed by **Or Chetrit**.

<!-- END_TF_DOCS -->
